[package]
name = "difftest"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Differential testing harness: Python reference vs Rust implementation (RUST_REWRITE_PLAN §4)."
publish = false

[lints]
workspace = true

[dependencies]
medium-doc = { path = "../../crates/medium-doc" }
medium-render = { path = "../../crates/medium-render" }
serde = { workspace = true }
serde_json = { workspace = true }
# The render gate's normaliser. Both sides are canonicalised by this one
# implementation, so it is a build dependency of the harness and not of any
# crate that ships.
html5ever = { workspace = true }
markup5ever_rcdom = { workspace = true }
