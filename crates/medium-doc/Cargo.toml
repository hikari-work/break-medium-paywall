[package]
name = "medium-doc"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Medium GraphQL JSON -> Document IR, plus URL resolution. Phase 1 of the Rust rewrite."
publish = false

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }
regex = { workspace = true }
url = { workspace = true }
form_urlencoded = { workspace = true }
psl = { workspace = true }
# `LinkResolver` is a network seam — see `resolve.rs` — and the only way to keep
# it object-safe behind a `&dyn` is to box the future.
async-trait = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }
